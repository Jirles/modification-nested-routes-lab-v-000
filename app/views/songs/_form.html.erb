<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <% if @song.artist.nil? %>
    <%= f.select options_from_collection_for_select(Author.all, :id, :name)%>
    <%= f.hidden_field :artist_id, @artist.id %>
  <% else %>
    <%= f.hidden_field :artist_id, @artist.id %>
  <% end  %>
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>
  <br />

  <%= f.submit %>
<% end %>
